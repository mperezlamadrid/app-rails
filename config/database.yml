default: &default
  adapter: postgresql
  encoding: unicode
  host: localhost
  username: manuelperez
  pool: 5
  timeout: 5000

development:
  <<: *default
  database: manuel_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: manuel_test

production:
  adapter: postgresql
  database: manuel_production
